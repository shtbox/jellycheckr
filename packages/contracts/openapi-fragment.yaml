openapi: 3.0.3
info:
  title: jellycheckr AYSW fragment
  version: 0.1.0
paths:
  /Plugins/Aysw/config:
    get:
      summary: Get effective config
  /Plugins/Aysw/sessions/{sessionId}/ack:
    post:
      summary: Acknowledge prompt action
  /Plugins/Aysw/sessions/{sessionId}/interaction:
    post:
      summary: Report user interaction
  /Plugins/Aysw/sessions/{sessionId}/prompt-shown:
    post:
      summary: Mark prompt displayed to user
  /Plugins/Aysw/web-client/register:
    post:
      summary: Register a web client session and disarm fallback for that session
  /Plugins/Aysw/web-client/heartbeat:
    post:
      summary: Renew a registered web client session lease
  /Plugins/Aysw/web-client/unregister:
    post:
      summary: Clear a registered web client session lease
  /Plugins/Aysw/admin/config:
    get:
      summary: Get admin config
    put:
      summary: Update admin config
